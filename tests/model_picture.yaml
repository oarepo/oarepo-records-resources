model:
  properties:
    metadata:
      properties:  # to add yout own properties, remove this line and uncomment the next one
        filename:
          type: fulltext
        alt:
          type: fulltext
  use:
    - invenio
  package: model_picture
  schema-server: 'local://'
